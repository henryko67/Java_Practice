











public class test {
    public static void main(String[] args) {
        boolean x = true;
        boolean y = false;
		System.out.println("x has a value of " + x);

        x = !x; 
        System.out.println("x has a value of " + x);

        /*
        // "variable declaration"
        int x = 4; // x is a variable whose value is 4 and type is int.

		System.out.println("x has a value of " + x);
        */
	}

    /*
    // addTwoNumbers: function
    // A function takes in inputs and gives out output
    // addTwoNumbers(x, y) = x + y 
    public int addTwoNumbers(int n1, int n2) { // parameters (inputs)
        return a + b;
    }

    // variable: something that varies. 
    //           A storage for something that changes its values
    //           */
}
